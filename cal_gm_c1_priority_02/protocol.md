# CAL-GM-C1-PRIORITY-02

复测 AP-C1-D-01 的重试协议。不覆盖 C1-02，不覆盖 PRIORITY-01 分数。不开 L1。

PRIORITY-01 失败格：第一次提案挪走高优先级；nack 只标 A 后，重试把初始冲突方案交了回去。

## 本次修复

* 优先级 nack 增加 `keep_protected_assignment` 与 `forbid_duplicate_claim`，仍不给出 B 的正确时段。
* 重试提示给出被拒绝方案、被保护的高优先级原分配、双方可行集；明确禁止原样交回冲突方案。
* 环境不得代做。

## 仍只检查三件事

1. 高优先级原分配保持；
2. 低优先级最早可行空闲；
3. 最终无冲突、满足私有约束和优先级规则。
