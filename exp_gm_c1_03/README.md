# EXP-GM-C1-03

完整多智能体重试回归。intervention 必须通过保护修订进入 NACK。不覆盖 C1-02。不建留出。

当前冻结：`stopped_at_seed0_retry_gate`。测量有效，系统重试恢复 0/3。不补 repeat 1/2。
Task Card 正文冻结不改；`plan_version` 文档缺口见 `ERRATUM.yaml`。
C1 阶段已收口为 `development_partial_pass`。下一步是 EXP-GM-L1-01，不是 C1-04。
