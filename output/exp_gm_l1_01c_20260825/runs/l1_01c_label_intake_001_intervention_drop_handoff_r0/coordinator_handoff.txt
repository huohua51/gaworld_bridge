【任务】三阶段标签接收核对入库
【步骤】[{"id": "label_rx51", "title": "接收"}, {"id": "label_ck52", "title": "核对"}, {"id": "label_st53", "title": "入库"}]
【规则】步骤必须按顺序执行。已完成步骤不得重复、不得覆盖。检查点由平台盖章为 ckpt-001。接替者必须读取同一版本检查点，从下一个未完成步骤继续。Coordinator 只确认检查点并指定接替者，不能代为执行。
【输出字段】接收 output 只能包含 received_ids 与 batch。 核验必须沿用已接收 ID：verified_ids 等于 received_ids，missing_ids 为空列表。 禁止编造未出现的 ID。不要计算 checksum。 入库 output 只能包含 bin 与 sealed，sealed 必须为 true。

只输出一个 JSON 对象。不要输出解释、Markdown 或其它文字。
你是 Coordinator。读取检查点，指定接替者与恢复步骤。不能执行具体步骤，不能给出剩余步骤的正确业务输出。
resume_step 必须是有序步骤中第一个不在 completed_steps 里的 id。不要根据 outputs 的键判断哪些步骤已完成。
不要重复已完成步骤，不要跳过中间步骤。remaining_steps 必须是全部尚未完成的步骤，按原顺序，且第一项等于 resume_step。
输出 {"successor": "worker_a|worker_b", "checkpoint_version": "ckpt-001", "completed_steps": [...], "resume_step": "<id>", "remaining_steps": [...]}。
【有序步骤】["label_rx51", "label_ck52", "label_st53"]
【已送达检查点】{"checkpoint_version": "ckpt-001", "completed_steps": ["label_rx51"], "outputs": {"label_rx51": {"received_ids": ["L9", "L10"], "batch": "T2"}}, "world_digest": "5ce11a67cdddc25d34555472c12c16d4703832d699aee34d0a81ced5af848e98"}
【状态】worker_a_offline_lb_v1。Worker A 已不可用，必须指定 worker_b 接替。续做位置仍由 completed_steps 决定。
