# 功能侧开发集总表（冻结）

- 时间：2026-08-25
- 模型：GLM-4-Flash，temperature=0
- ranking_eligible：false
- 留出：未开始
- 完成标准：可解释状态，不是满分

功能侧做完，不等于所有任务都要拿满分。只要测量有效、任务可做、失败能够定位并产生明确改进项，即使结果是失败，这一类功能也已经完成评测。

## 各功能模块

| 功能模块 | 结论 | 评测 | 平台改进 | 开发集结局 |
|---|---|---|---|---|
| T3 审核与协作 | 独立 Reviewer 私有信息有价值；Multi=1.0，Single/Drop=0.5 | 开发集完成 | 主要闭环 | pass |
| I1 核实信息传播 | Full 通过，Drop/NoVerify 失败，权限隔离有效 | 完成 | 已闭环 | pass |
| REL1 来源可靠性更新 | 平台能传播状态，Agent 不会让最新结果覆盖旧多数 | 已得到可解释失败 | 尚未改进闭环 | fail |
| N1 | Direct/Full 共同约 0.333 地板，无法识别通信或多体价值 | 构念未测开 | 不适用 | **retired** |
| C1 集体协调 | 能消除冲突、满足私有约束；政策部分失败，NACK 重试 0/3 | 可解释的部分通过 | 重试和版本管理未闭环 | partial_pass |
| L1 中断恢复 | Full Multi 18/18；两种 Drop 中断格均 0/9 | 开发集完成 | 已闭环 | pass |
| 测量系统 | R0–R3、权限、Drop、Trace、首错总体可用 | 基本完成 | 两个小缺口 | partial_pass |
| 留出泛化 | 所有结果基本仍是开发集 | **未开始** | 无正式排名资格 | N/A |

N1 分数保持冻结，不补 Seed，不在共同地板上计算 Multi-Agent Benefit，不建 N1-02。构念由 I1（核实传播）和 REL1（最新状态更新）覆盖。

L1-01b StrictPair 仍为 2/3，不得回改。L1 开发集通过以 L1-01c 为准。

## 仍开放的系统问题

```text
C1 NACK：平台成功触发 NACK → 模型三次进入重试 → 0/3 形成正确终局
  AP-C1-D-01 收到拒绝后没有正确调整联合方案
  AP-C1-F-01 让模型负责 plan_version 握手不合理

REL1：latest_is_binding=true 时仍按旧多数行动（AP-REL1-01）
```

这两项不阻碍报告功能评测结果，但阻碍说“GAWorld 已经改好”。C1 冻结为测量有效的功能失败。REL1 未做窄组件校准。

## 测量缺口

1. `first_error_node` 必须覆盖所有失败条件，不能再出现 FullPass=0 但 first_error=none。L1-01b 离心转子 control 已有不改分勘误。
2. 平台应管理 `plan_id/spec_version`，模型只提交业务方案。
3. `typed_patch` 登记为退役协议。不为把 04e-E 修到满分继续投入。04e 分数不变。

## 留出资格

| 功能族 | 可否建留出 |
|---|---|
| T3 | 可以，尚未建 |
| I1 | 可以，尚未建 |
| L1 | 可以，尚未建 |
| C1 | 暂不可以 |
| REL1 | 暂不可以 |
| N1 | 不可以（已退役） |

## 进度

- 第一轮开发性功能诊断：约 85%。机制覆盖已经基本完成。剩余是 `first_error` 枚举和平台版本管理。
- 正式 Benchmark：不能按 85% 理解。留出题、跨任务泛化、跨模型稳健性还没有完成。

下一步：修两个测量/平台缺口，再为 T3、I1、L1 制作密封留出集。留出只跑一次，不再调协议。
