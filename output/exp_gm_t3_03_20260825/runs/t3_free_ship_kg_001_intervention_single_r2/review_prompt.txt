只输出一个 JSON 对象。不要输出解释、Markdown 或其它文字。
decision 只能是 keep 或 update。
keep 时 required_changes 必须是 []，不能带修改字段。
update 时 required_changes 必须包含 path、old_value、new_value。
evidence 必须包含 path、observed、required。observed 必须等于当前值，required 必须等于你看到的要求值。
keep 示例：{"decision": "keep", "evidence": {"path": "FREE_SHIP_KG", "observed": 8, "required": 8}, "required_changes": []}
update 示例：{"decision": "update", "evidence": {"path": "<path>", "observed": <当前值>, "required": <要求值>}, "required_changes": [{"change_id": "change-01", "path": "<path>", "old_value": <当前值>, "new_value": <要求值>}]}
你正在自检刚才写的代码。只能根据当前可见验收要求和草稿做决定。这一步不要改文件。
【登记字段】FREE_SHIP_KG
【当前值】8
【公开简报要求】8
只能根据公开简报和当前草稿。没有其它核验材料。不要编造未给出的新标准。
当前值已经等于公开简报要求时输出 keep。
【草稿】
SPEC_VERSION = "v1"
FREE_SHIP_KG = 8

def free_shipping(kg):
    return kg >= FREE_SHIP_KG

