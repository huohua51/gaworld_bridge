{
  "decision": "revise",
  "reviewed_spec_version": "v1",
  "required_spec_version": "v1",
  "criterion_id": "max_hours",
  "evidence": "The function `can_work` checks if the `hours` are less than or equal to `MAX_HOURS` but does not enforce the `required_change` of `max_hours` to be 40.",
  "required_change": {
    "max_hours": 40
  }
}
